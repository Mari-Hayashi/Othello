using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum CellState { Black, White, Empty, MAX }
public enum HorizontalDirection { Right, Left, None, MAX }
public enum VerticalDirection { Up, Down, None, MAX }
